(function($) {
/**
 * Implementation of Drupal behavior.
 */
Drupal.openlayers.addBehavior('core44_ol_pimp_behavior_mapwidthtoggle', function (data, options) {
	var map = data.openlayers;
	//console.log(map);

 		
			         


/*
		console.log(lessPage.div.innerHTML);	  
		//-----you must create a panel to add buttons in it
		var panel = new OpenLayers.Control.Panel({defaultControl: morePage});
		//-------you must add the button to the panel
		panel.addControls([lessPage,morePage]);	
		map.addControl(panel);	
*/

			

});//end behavior
})(jQuery);
